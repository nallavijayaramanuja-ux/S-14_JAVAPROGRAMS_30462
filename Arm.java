import java.util.Scanner;

		Class Arm
		public Static void main (String args (){
  		{

		int no, temp,rev=0

		Scanner Sc = new Scanner (system.in);
		system.out.println (" please enter the number);
		no= Sc.nextInt();
		while(no>0)
		{
		x=no%10;
		rev=rev+(x*x*x);
		no=no/10;
		
		}

		if (rev==temp)
		 system.out.println("the entered number " + " temp" + is amstorng };
		esle 
		system.out.println{"the entered number " + " temp"+ is not amstrong }"

  	 }
} 

